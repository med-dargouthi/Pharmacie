{% extends 'base.html.twig' %}


{% block body %}
    <div class="container mt-4">
        <h1 class="text-center mb-4">Edit LigneBonDeCommande</h1>
        <div class="card p-4 shadow-lg rounded">
            {{ include('ligne_bon_de_commande/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <div class="mt-3 text-center">
            <a href="{{ path('app_ligne_bon_de_commande_index') }}" class="btn btn-outline-primary">Back to list</a>
        </div>

        <div class="mt-4 text-center">
            {{ include('ligne_bon_de_commande/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
