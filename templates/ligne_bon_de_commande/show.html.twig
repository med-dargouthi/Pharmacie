{% extends 'base.html.twig' %}

{% block title %}LigneBonDeCommande{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="text-center mb-4">LigneBonDeCommande Details</h1>
        <div class="card p-4 shadow-lg rounded">
            <table class="table table-striped table-bordered">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ ligne_bon_de_commande.id }}</td>
                    </tr>
                    <tr>
                        <th>Nom</th>
                        <td>{{ ligne_bon_de_commande.nom }}</td>
                    </tr>
                    <tr>
                        <th>Email</th>
                        <td>{{ ligne_bon_de_commande.email }}</td>
                    </tr>
                    <tr>
                        <th>NumTel</th>
                        <td>{{ ligne_bon_de_commande.numTel }}</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <div class="text-center mt-4">
            <a href="{{ path('app_ligne_bon_de_commande_index') }}" class="btn btn-outline-primary">Back to list</a>
            <a href="{{ path('app_ligne_bon_de_commande_edit', {'id': ligne_bon_de_commande.id}) }}" class="btn btn-warning btn-sm">Edit</a>
        </div>

        <div class="mt-4 text-center">
            {{ include('ligne_bon_de_commande/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
